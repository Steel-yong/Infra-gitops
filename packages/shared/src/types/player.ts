// 플레이어(유저 본인) 실시간 위치 타입 (0~1 정규화 게임 좌표 + 방향)

export interface PlayerPosition {
  /** 플레이어 위치 X (0~1 정규화, 이미지 좌표계 — 좌 0 → 우 1) */
  x: number;
  /** 플레이어 위치 Y (0~1 정규화, 이미지 좌표계 — 위 0 → 아래 1) */
  y: number;
  /** 바라보는 방향 (도, 0=북, 시계방향 증가). 화살표 미검출 시 생략. */
  heading?: number;
}
