# PUBG Helper — Claude Code 바이블

---

# 1부: 작업 원칙

## 작업 순서 (반드시 이 순서대로)

### 1단계: 코드베이스 읽기
작업 시작 전 반드시 관련 파일 전체를 읽어라.
읽지 않고 추측해서 작성하지 마라.
가정한 내용을 명확하게 밝혀라. 확실하지 않으면 질문해라.
여러 가지 해석이 가능하다면 묵묵히 선택하지 말고 모두 제시해라.
더 간단한 방법이 있다면 언급해라. 필요하다면 반박해라.
이해가 안 되는 부분이 있으면 멈춰라. 무엇이 헷갈리는지 말하고 질문해라.

### 2단계: Plan 수립
/ce-plan 실행 → 코드베이스 기반으로 상세 계획 수립.
계획 없이 코딩 시작 금지.
계획에 반드시 포함할 것들.
- 작업 범위.
- 영향받는 파일 목록.
- 구현 순서.
- 검증 방법.

다단계 작업은 계획을 이렇게 명시해라.
- 1. [단계] → 검증: [확인 방법].
- 2. [단계] → 검증: [확인 방법].
- 3. [단계] → 검증: [확인 방법].

명확한 성공 기준이 있으면 독립적으로 반복할 수 있다. "그냥 작동하게 해라" 같은 모호한 기준은 지속적인 확인을 요구한다.

작업 전 반드시 세 가지 산출물을 만들어라. 없으면 코딩 시작하지 마라.
- 계획: 무엇을 왜 만드는지.
- 체크리스트 (checklist.md): 체크박스로 된 구체적인 작업. 완료될 때마다 체크.
- 컨텍스트 노트 (context-notes.md): 작업 중 내린 결정과 그 이유. 계속 추가.

사용자가 계획만 주고 바로 코딩 시작하라고 하면 멈추고 물어라.
"체크리스트와 컨텍스트 노트를 먼저 만들까요?"
다음 세션은 모든 결정을 다시 도출하지 않고 이어가려면 그 노트가 필요하다.

### 3단계: Linear 이슈 생성
계획을 작은 단위로 쪼개서 Linear 이슈화.
이슈 하나 = 하루 이내 완료 가능한 크기.

올바른 쪼개기 예시.
- PUB-01: shared CircleData 타입 정의.
- PUB-02: captureWorker 기본 구조 (getDisplayMedia 연결).
- PUB-03: 전체맵 열림 감지 로직.
- PUB-04: 자기장 원 추출 로직 (Hough Circle).
- PUB-05: WebSocket Gateway 연결.
- PUB-06: 각 로직 유닛 테스트 작성.

이슈 단위로 작업해라. 한 번에 여러 이슈 섞지 마라.

### 4단계: git worktree로 브랜치 작업
모든 브랜치 작업은 반드시 git worktree로 한다.
```bash
1. git worktree add ../feature-{이슈번호} feature/{이슈번호}
2. cd ../feature-{이슈번호}
3. 여기서 작업
```
메인 폴더는 항상 clean 상태 유지
브랜치마다 별도 폴더. 충돌 없이 병렬 작업 가능.
브랜치 전환 없이 여러 이슈 동시에 진행 가능.

### 5단계: 구현 → 테스트 반복
구현하면 반드시 테스트 실행.
테스트 깨지면 이유 분석하고 수정.
수정하면 다시 테스트.
커버리지 95% 달성까지 반복.
절대 테스트 실패 상태로 다음 단계 넘어가지 마라.

과제를 검증 가능한 목표로 전환해라.
- "유효성 검사 추가" → "유효하지 않은 입력에 대한 테스트 작성 후 통과시켜라".
- "버그 수정" → "버그를 재현하는 테스트 작성 후 통과시켜라".
- "리팩토링" → "리팩토링 전후 테스트 모두 통과 확인".

### 6단계: 커밋 → PR → 배포
논리적 변경 하나 완료 시 즉시 커밋해라. 사용자가 요청할 때까지 기다리지 마라.
시맨틱 커밋: "feat: 전체맵 열림 감지 로직 구현".
PR 오픈 → CI 통과 (커버리지 95% 미달 시 블락) → ArgoCD 자동 배포.

커밋 기준.
- "이 커밋을 한 문장으로 설명할 수 있나?" 가능하면 커밋. 불가능하면 변경이 섞인 것이므로 분리해라.
- 좋음: "feat: auth 미들웨어 추가".
- 나쁨: "auth 추가하고 UI도 고치고 버그도 수정" → 3개로 분리해라.
- 관련 없는 수정 20개를 쌓아서 개별 롤백 능력을 잃지 마라.

---

## 코딩 원칙

### 기본 규칙
- TypeScript strict mode 필수.
- any 타입 절대 금지.
- 모든 public 함수 JSDoc 작성.
- console.log 커밋 금지.
- 에러 처리 필수 (try/catch).
- 함수형 컴포넌트 + hooks 사용.

### 단순함 우선
문제를 해결하는 최소한의 코드만 작성해라. 추측성 코드는 일절 포함하지 마라.
- 요청한 기능 외에 추가 기능 없음.
- 일회용 코드에 추상화 없음.
- 요청하지 않은 유연성이나 설정 가능성 없음.
- 불가능한 시나리오에 대한 에러 처리 없음.
- 200줄을 썼는데 50줄로 가능하면 다시 써라.
- "시니어 엔지니어가 이게 과하다고 할까?" 그렇다면 단순하게 해라.

### 외과적 변경
꼭 필요한 것만 건드려라. 네가 만든 것만 정리해라.

기존 코드 수정 시.
- 인접한 코드, 주석, 포맷을 개선하지 마라.
- 멀쩡한 것을 리팩토링하지 마라.
- 네 방식이 달라도 기존 스타일을 맞춰라.
- 관련 없는 죽은 코드 발견 시 언급만 해라. 삭제 금지.

네 변경이 고아를 만들 때.
- 네 변경으로 인해 사용되지 않게 된 import/변수/함수는 제거해라.
- 기존에 있던 죽은 코드는 요청받지 않으면 제거하지 마라.
- 테스트: 변경된 모든 줄이 사용자의 요청에 직접 연결되어야 한다.

### 새 파일 첫 줄 한국어 주석 필수
모든 새 소스 파일 첫 줄에 해당 파일의 역할을 한 줄 한국어 주석으로 써라.
```typescript
// 화면 캡처 프레임에서 자기장 원을 추출하는 서비스
```
```python
# 대회 영상에서 프로 선수 위치 좌표를 추출하는 파이프라인
```
필수 디렉티브('use client', 'use server', shebang) 바로 아래에 위치.
설정 파일(*.config.ts, package.json 등)은 생략.

이유: 에이전트는 전체 코드베이스가 아닌 파일을 선택적으로 읽는다. 한 줄 한국어 헤더가 즉각적인 컨텍스트를 제공하여 다음 세션이 전체 파일을 다시 읽지 않고도 탐색할 수 있다.

### 한국어 출력 규칙
사용자가 한국어로 쓰면 출력도 한국어로.
한국어 문장은 마침표로 끝내라. 콜론 아님.
콜론은 코드, 키-값 쌍, 라벨 안에서만 사용.
모든 한국어 문장 종결부는 ., ?, ! 여야 한다.

## 작업 완료 후 플로우

커밋 완료 후:
1. "PR 생성할까요?" 물어보기
2. Yes → push → GitHub PR 생성
3. PR URL 보여주기
4. "PR 머지 후 worktree 삭제할까요?" 물어보기
5. Yes → git worktree remove ../feature-{번호}
6. "다음 이슈 {번호} 시작할까요?" 물어보기
---

## 테스트 원칙

### 커버리지 95% 필수
새 기능은 테스트 먼저 작성.
커버리지 95% 미달 시 PR 자동 블락.

### 유닛 테스트 (Vitest)
모든 함수 단위 테스트.
외부 의존성은 mock 처리.
엣지 케이스 반드시 포함.

### E2E 테스트 (Playwright)
Playwright 화면 직접 띄워서 실제 동작 확인.
화면공유 플로우 전체 자동 테스트.
OCR 인식 결과 스크린샷 비교.
테스트 실패 시 스크린샷 자동 저장.

### 테스트 실행 명령어
```bash
pnpm test            # 유닛 테스트
pnpm test:coverage   # 커버리지 확인
pnpm test:e2e        # Playwright E2E
```

"완료"라고 말하기 전에 반드시 테스트를 실행해라.
테스트가 통과되면 결과를 보고해라. 실패하면 수정 후 다시 실행해라.
테스트 환경이 없으면 최소한 프로젝트가 빌드/컴파일되는지 확인해라.
이는 LLM이 가장 자주 건너뛰는 단계다. 절대 타협 불가.

---

## 에러 처리 원칙
뭔가 실패했을 때 반드시 이 순서로 처리해라.
- 전체 에러 메시지와 스택 트레이스를 읽어라.
- 로그에 출력된 실제 내용을 확인해라. 예상 내용이 아니라.
- 원인을 확인하기 전에 흔한 수정을 적용하지 마라.
- 불명확하면 로그를 추가해서 상태 확인 후 수정해라.

패턴 매칭으로 추측하지 마라.
에러 키워드 보고 추측해서 최근 패턴 수정을 적용하면 한 줄 버그가 세 파일 리팩토링이 된다.
이는 LLM이 "테스트 실행" 다음으로 가장 자주 건너뛰는 단계다.

---

## 절대 하지 말 것
- 코드베이스 읽지 않고 작업 시작.
- 계획 없이 코딩 시작.
- Linear 이슈 없이 작업 시작.
- 테스트 없는 PR.
- 커버리지 95% 미달 상태로 PR.
- 테스트 실패 상태로 완료 선언.
- any 타입 사용.
- 하드코딩된 시크릿.
- console.log 커밋.
- 요청하지 않은 기능 추가.
- 멀쩡한 코드 리팩토링.
- 원인 확인 전 수정 시도.
- 인접한 코드 무단 수정.


# 2부: 프로젝트 정보

## 프로젝트 개요
배틀그라운드 게임 보조 웹서비스.
- 화면공유 하나로 전체맵 자기장 자동 분석 + 프로 위치 추천 + 자기장 알림 제공.

---

## 모노레포 구조
Infra-gitops/
├── apps/
│   ├── frontend/              # Next.js 14 (프론트엔드)
│   └── services/
│       ├── capture/           # 화면캡처 + 이미지분석 (NestJS)
│       ├── location/          # 프로 위치 추천 (NestJS)
│       └── alert/             # 자기장 타이머 + 알림 (NestJS)
├── packages/
│   └── shared/                # 공통 타입, DTO, 인터페이스
├── clusters/                  # K8s 매니페스트 (기존 인프라)
├── applicationsets/           # ArgoCD AppSet (기존 인프라)
├── pnpm-workspace.yaml
└── CLAUDE.md

---

## 기술 스택

| 분류 | 기술 | 이유 |
|------|------|------|
| 언어 | TypeScript 전체 | Claude Code 단일 컨텍스트 |
| 프론트 | Next.js 14 (App Router) | SSR + 실시간 처리 |
| 지도 | Leaflet.js | 오픈소스, 커스텀 오버레이, 휠 줌 지원 |
| 화면인식 | Screen Capture API + Canvas | 젬파고 동일 방식, 브라우저 네이티브 |
| OCR | Tesseract.js (Web Worker) | 숫자+콜론 전용, 브라우저 실행 |
| 백엔드 | NestJS | TypeScript, WebSocket 기본 지원 |
| 실시간 통신 | WebSocket (Socket.io) | 프레임 분석 결과 실시간 전송 |
| 이미지 처리 | Sharp | 서버사이드 이미지 분석 |
| DB | PostgreSQL + Prisma | 위치 데이터, 세션 관리 |
| 알림 | Web Notifications API | 브라우저 네이티브 알림 |
| 모노레포 | pnpm workspace | 패키지 공유, 중복 설치 방지 |
| AI (Phase 6) | Python FastAPI | 프로 위치 학습 (별도 마이크로서비스) |

---

## 서비스별 역할

### frontend (Next.js 14)
- 맵 선택 UI (에란겔/미라마/태이고/론도) — 사용자가 직접 선택.
- 화면공유 시작/중지 버튼 (getDisplayMedia API).
- Leaflet.js 지도 + 자기장 원 오버레이 + 휠 확대/축소.
- 프로 위치 마커 표시 (S/A/B 등급별, 자기장 중심 거리순 랭킹).
- 체크박스 알림 설정 (30초/20초/10초 독립 선택).
- Web Workers로 캡처/OCR 처리 (메인 스레드 블락 방지).

### capture-service (NestJS)
- WebSocket Gateway로 프론트에서 프레임 수신.
- 전체맵 열림 감지: 화면 중앙 파란색 비율 30% 초과 시 판단.
- 자기장 원 추출: 흰색 테두리 픽셀 마스킹 → Hough Circle Transform → 중심(x,y) + 반경(r).
- 맵 좌표계로 정규화 (0~1).
- 분석 결과 WebSocket으로 프론트 전송.

### location-service (NestJS)
- 자기장 원 중심(x,y) + 반경(r) 입력받아 프로 추천 위치 반환.
- DB에서 원 안의 위치 필터링.
- 자기장 중심 거리순 정렬.
- S/A/B 등급별 마커 데이터 반환.

### alert-service (NestJS)
- 미니맵 위 타이머 영역 크롭 (해상도별 좌표 고정).
- Tesseract.js OCR: whitelist 0123456789: → "1:38" → 총 초(98초) 변환.
- 빨간 느낌표 픽셀 감지로 자기장 상태 구분.
  - 느낌표 있음: 자기장 줄어드는 중 → 30/20/10초 알림 트리거.
  - 느낌표 없음: 다음 자기장 대기 중 → 알림 없음, 남은 시간 표시.
- Web Notifications API로 브라우저 알림 발송.

### shared (packages/shared)
- CircleData: 자기장 원 중심(x,y) + 반경(r) 타입.
- MapType: 에란겔/미라마/태이고/론도 enum.
- LocationData: 프로 추천 위치 타입 (좌표, 등급, 팀명 등).
- TimerState: 자기장 타이머 상태 타입.
- 프론트/백엔드 공통으로 import해서 사용.

---

## 인프라 정보
- K8s Master: 172.20.0.10 / Worker-1: 172.20.0.11 / Worker-2: 172.20.0.12.
- Harbor: harbor.도메인/pubg-helper/ (이미지 레지스트리).
- ArgoCD: GitOps 자동 배포, Sync-Wave 50 (앱 배포 순서).
- Envoy Gateway: 외부 트래픽 라우팅 (172.20.0.100).
- Vault + ESO: 시크릿 관리 (DB 패스워드, API 키 등).
- Longhorn: PVC 스토리지.
- Cilium: CNI + NetworkPolicy.