# PUBG Helper — Claude Code 바이블

> 이 문서는 모든 작업 시작 시 자동 로드된다. **0부 세션 시작 프로토콜부터 읽어라.**

---

## 목차
- [0부. 세션 시작 프로토콜](#0부-세션-시작-프로토콜)
- [1부. 절대 규칙 (DO / DON'T)](#1부-절대-규칙-do--dont)
- [2부. 작업 흐름](#2부-작업-흐름)
- [3부. 코딩 스타일](#3부-코딩-스타일)
- [4부. 문서화 규칙](#4부-문서화-규칙)
- [5부. 프로젝트 정보](#5부-프로젝트-정보)

---

# 0부. 세션 시작 프로토콜

**새 세션 시작 시 무조건 이 순서로 확인한 뒤 사용자에게 보고한다. 확인 없이 작업 시작 금지.**

1. `docs/projects/` 진행 중인 계획 파일 모두 읽기.
2. Linear에서 "In Progress" 상태 이슈 조회.
3. `git worktree list` — 열려 있는 워크트리 목록.
4. `git log origin/develop..develop --oneline` — 미푸시 커밋 확인.
5. `git status` — 미커밋 변경 확인.

확인 후 다음 형식으로 보고한다.

```
현재 진행 상태:
- 계획: [docs/projects/파일명]의 [어디까지]
- 작업 중 이슈: PUB-XX
- 미푸시 develop 커밋: N개
- 열린 워크트리: feature-XX, feature-YY
다음 할 일: [구체적 작업]
계속 진행할까요?
```

---

# 1부. 절대 규칙 (DO / DON'T)

## ✅ MUST DO

- 작업 전 관련 파일 전체 읽기.
- 작업 전 계획·체크리스트·컨텍스트 노트 3종 산출물 작성.
- 모든 작업은 Linear 이슈와 연결.
- 모든 브랜치 작업은 `git worktree`로. **메인 폴더는 항상 clean 상태 유지.**
- 모든 PR base는 `develop` 브랜치.
- 새 소스 파일 첫 줄에 한국어 한 줄 주석.
- 사용자 결정 필요 시 HTML 시각화부터 만들기.
- "완료" 선언 전에 반드시 테스트 실행 + 결과 보고.
- **논리적 변경 하나 완료 시 즉시 커밋** (사용자 요청 기다리지 마라).
- 변경 하나 = 커밋 하나 (한 문장으로 설명 가능해야 함).
- 한국어 문장은 마침표(.), 물음표(?), 느낌표(!)로만 끝.

## 🗣️ 대화 원칙

- **확실하지 않으면 질문해라.** 가정한 내용은 명확히 밝혀라.
- **여러 해석 가능 시 묵묵히 선택하지 마라.** 모두 제시하고 사용자에게 선택권.
- **더 간단한 방법이 있다면 반드시 언급해라.** 사용자 안이 과해 보이면 반박해라.
- **이해 안 되면 멈춰라.** 뭐가 헷갈리는지 말하고 질문해라.

## ❌ MUST NOT

- 코드베이스 읽지 않고 추측해서 작성.
- 계획·이슈·테스트 없이 코딩 시작.
- `main` 브랜치에 직접 머지 (사용자 명시 요청 시에만).
- 테스트 실패 상태로 다음 단계 진행 / "완료" 선언.
- `any` 타입 사용.
- `console.log`, 하드코딩 시크릿 커밋.
- 요청하지 않은 기능 추가 · 멀쩡한 코드 리팩토링.
- 인접한 코드·주석·포맷 무단 수정.
- 원인 파악 전에 흔한 수정 시도 (패턴 매칭 추측 금지).
- 커버리지 95% 미달 상태로 PR 오픈.

---

# 2부. 작업 흐름

## 단계별 흐름

```
세션 시작 프로토콜 (0부)
   ↓
1. 코드베이스 읽기 (관련 파일 전체)
   ↓
2. /ce-plan 으로 계획 수립 + checklist.md + context-notes.md
   ↓
3. Linear 이슈로 쪼개기 (1이슈 = 1일 이내)
   ↓
4. git worktree로 브랜치 분리
   ↓
5. 구현 ↔ 테스트 반복 (커버리지 95% 달성까지)
   ↓
6. 논리 단위마다 즉시 커밋 → PR → develop 머지 → 워크트리 삭제
```

## 산출물 3종 (계획 단계)

| 파일 | 목적 |
|------|------|
| 계획 본문 | 무엇을 왜 만드는지 |
| `checklist.md` | 체크박스 작업 목록. 완료 시 즉시 체크 |
| `context-notes.md` | 작업 중 내린 결정과 이유. 다음 세션이 이어가는 근거 |

사용자가 계획만 주고 코딩 시작하라 해도 멈추고 묻는다: **"체크리스트·컨텍스트 노트 먼저 만들까요?"**

## 다단계 작업 계획 형식

다단계 작업은 반드시 이 형식으로 명시한다. 각 단계마다 **검증 방법**이 있어야 한다.

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

명확한 성공 기준이 있으면 독립적으로 반복 가능하다. "그냥 작동하게 해라" 같은 모호한 기준은 지속적인 확인을 요구한다.

## 검증 가능한 목표로 전환

| 모호한 지시 | 검증 가능한 형태 |
|-----------|---------------|
| 유효성 검사 추가 | 유효하지 않은 입력 테스트 작성 → 통과 |
| 버그 수정 | 버그를 재현하는 테스트 작성 → 통과 |
| 리팩토링 | 리팩토링 전후 테스트 모두 통과 |

## 커밋 / PR / 워크트리

**커밋 기준:** "이 커밋을 한 문장으로 설명할 수 있나?" 가능 → 커밋. 불가능 → 분리.

**브랜치 규칙:** `feature/* → develop → (최종 완성 후) main`

**워크트리 사용법:**
```bash
git worktree add ../feature-{이슈번호} feature/{이슈번호}
cd ../feature-{이슈번호}
```

**커밋 완료 후 사용자에게 묻는 흐름:**
1. "PR 생성할까요?" → Yes → push + PR
2. "PR 머지 후 worktree 삭제할까요?" → Yes → `git worktree remove`
3. "다음 이슈 시작할까요?"

## 테스트 원칙

| 종류 | 도구 | 비고 |
|------|------|------|
| 유닛 | Vitest | 외부 의존성 mock, 엣지 케이스 필수 |
| E2E | Playwright | 화면공유 플로우 자동화, 실패 시 스크린샷 |
| 커버리지 | Vitest --coverage | **95% 미달 = PR 블락** |

```bash
pnpm test            # 유닛
pnpm test:coverage   # 커버리지
pnpm test:e2e        # E2E
```

"완료" 선언 전 무조건 테스트 실행. 테스트 환경 없으면 최소한 빌드/컴파일이라도 확인.

## 에러 처리 순서

뭔가 실패하면 반드시 이 순서.
1. 전체 에러 메시지 + 스택 트레이스 읽기.
2. 로그에 찍힌 실제 내용 확인 (예상 내용 아님).
3. 원인 확인 전 흔한 수정 적용 금지.
4. 불명확하면 로그 추가해서 상태 확인 → 수정.

키워드만 보고 패턴 매칭으로 추측 금지. 한 줄 버그가 세 파일 리팩토링이 된다.

---

# 3부. 코딩 스타일

## TypeScript 기본
- strict mode 필수, `any` 금지.
- 모든 public 함수에 JSDoc.
- 에러 처리 필수 (try/catch).
- React는 함수형 + hooks만.

## 단순함 우선
- 요청한 기능 외 추가 금지.
- 일회용 코드에 추상화 금지.
- 불가능한 시나리오 에러 처리 금지.
- 200줄 짠 게 50줄로 가능하면 다시 쓴다.
- "시니어가 과하다고 할까?" → 그렇다면 단순하게.

## 외과적 변경
- 꼭 필요한 것만 건드린다.
- 인접 코드·주석·포맷 무단 개선 금지.
- 기존 스타일이 내 방식과 달라도 맞춘다.
- 변경된 모든 줄이 사용자 요청에 직접 연결돼야 한다.

**고아 정리 규칙:**
- 내 변경으로 사용 안 되게 된 import/변수/함수 → 제거.
- 기존부터 있던 죽은 코드 → 요청 없으면 그대로 둔다.

## 새 파일 첫 줄 한국어 주석
모든 새 소스 파일 첫 줄에 역할을 한국어 한 줄로.
```typescript
// 화면 캡처 프레임에서 자기장 원을 추출하는 서비스
```
```python
# 대회 영상에서 프로 선수 위치 좌표를 추출하는 파이프라인
```
- `'use client'`, `'use server'`, shebang 바로 아래에 위치.
- 설정 파일(`*.config.ts`, `package.json`)은 생략.
- 이유: 에이전트는 파일을 선택적으로 읽는다. 한 줄 헤더가 다음 세션의 탐색 비용을 줄인다.

## 한국어 출력
- 사용자가 한국어로 쓰면 출력도 한국어.
- 모든 문장 종결은 `.`, `?`, `!`.
- 콜론(`:`)은 코드·키-값·라벨에만 사용. 문장 끝에 금지.

---

# 4부. 문서화 규칙

## docs/ PARA 구조
```
docs/
├── projects/     # 진행 중 프로젝트 계획 (완료 시 archives로 이동)
├── areas/        # 인프라/SSH/운영 등 지속 관리 영역
├── resources/
│   ├── solutions/  # 해결된 버그·패턴·컨벤션 (YAML 프론트매터: module, tags, problem_type)
│   └── mockups/    # HTML 시각화
└── archives/     # 완료/비활성 보관
```

**금지 경로:** `docs/plans/`, `docs/solutions/`, `docs/mockups/`, `docs/decisions/` — 모두 PARA로 통일.

## HTML 시각화 (결정 필요 시 필수)
사용자가 결정해야 하는 사항은 **텍스트 설명 금지.** 무조건 시각화 먼저.

**파일 경로:** `docs/resources/mockups/[날짜]-decision-[주제].html`

**포함 항목:**
- 선택지별 트레이드오프 비교표.
- 각 선택지의 영향 범위.
- 추천 선택지 + 이유.
- 결정 후 다음 액션.

**형식:** 자급자족 단일 HTML (CSS 인라인, 외부 의존성 없음), 다크 테마, 한국어 라벨.

---

# 5부. 프로젝트 정보

## 개요
배틀그라운드 보조 웹서비스. 화면공유 하나로 **전체맵 자기장 분석 + 프로 위치 추천 + 자기장 알림** 제공.

## 모노레포 구조
```
Infra-gitops/
├── apps/
│   ├── frontend/                 # Next.js 14 (App Router)
│   └── services/
│       ├── capture/              # 화면캡처 + 이미지분석 (NestJS)
│       ├── location/             # 프로 위치 추천 (NestJS)
│       └── alert/                # 자기장 타이머 + 알림 (NestJS)
├── packages/shared/              # 공통 타입·DTO·인터페이스
├── docs/{projects,areas,resources,archives}/
├── clusters/                     # K8s 매니페스트
├── applicationsets/              # ArgoCD AppSet
├── docker-compose.yml            # 로컬 개발 환경
└── pnpm-workspace.yaml
```

## 기술 스택
| 분류 | 기술 | 이유 |
|------|------|------|
| 언어 | TypeScript 전체 | Claude Code 단일 컨텍스트 |
| 프론트 | Next.js 14 (App Router) | SSR + 실시간 |
| 지도 | Leaflet.js (CRS.Simple) | 커스텀 오버레이, 휠 줌 |
| 화면인식 | Screen Capture API + Canvas | 브라우저 네이티브 |
| OCR | Tesseract.js (Web Worker) | 숫자+콜론 전용 |
| 백엔드 | NestJS | TS + WebSocket 기본 지원 |
| 실시간 | WebSocket (Socket.io) | 프레임 분석 결과 |
| 이미지 처리 | Sharp | 서버사이드 분석 |
| DB | PostgreSQL + Prisma | 위치/세션 |
| 모노레포 | pnpm workspace | 패키지 공유 |
| AI (Phase 6) | Python FastAPI | 프로 위치 학습 |

## 서비스별 역할

**frontend (Next.js 14)**
- 맵 선택 UI (에란겔/미라마/태이고/론도).
- 화면공유 시작/중지 (`getDisplayMedia`).
- Leaflet 지도 + 자기장 원 오버레이 + 휠 줌.
- 프로 위치 마커 (S/A/B 등급, 자기장 중심 거리순).
- 알림 설정 (30/20/10초 독립 체크박스).
- Web Workers로 캡처/OCR 처리.

**capture-service (NestJS, :3001)**
- WebSocket Gateway로 프레임 수신.
- 전체맵 열림 감지 (청록 픽셀 3% 이상).
- 자기장 원 추출 (흰 픽셀 → Kasa 원 피팅 → 0~1 정규화).
- 분석 결과를 WebSocket으로 프론트 전송.

**location-service (NestJS, :3002)**
- 자기장 원(x,y,r) 입력 → 원 안 프로 위치 필터링 + 거리순 정렬.
- S/A/B 등급별 마커 반환.

**alert-service (NestJS, :3003)**
- 미니맵 타이머 영역 크롭 → Tesseract OCR.
- 빨간 느낌표 픽셀로 자기장 상태 구분.
  - 있음: 줄어드는 중 → 30/20/10초 알림.
  - 없음: 다음 자기장 대기 → 알림 없음.
- Web Notifications API로 발송.

**shared (packages/shared)**
- `CircleData` (x, y, r 0~1 정규화).
- `MapType` enum.
- `LocationData` (좌표, 등급, 팀명).
- `TimerState`.
- `SocketEvents`.

## 인프라
- K8s: Master 172.20.0.10 / Worker-1 172.20.0.11 / Worker-2 172.20.0.12.
- Harbor: 이미지 레지스트리.
- ArgoCD: GitOps 자동 배포 (Sync-Wave 50).
- Envoy Gateway: 외부 트래픽 (172.20.0.100).
- Vault + ESO: 시크릿 관리.
- Longhorn: PVC 스토리지.
- Cilium: CNI + NetworkPolicy.

## 좌표계 주의사항
- `CircleData.y`, `LocationData.coordY` → 이미지 좌표계 (위 0 → 아래 1).
- Leaflet `CRS.Simple` → lat 위로 증가 (아래 0 → 위 1).
- **Marker는 `[1 - y, x]`로 변환 필요.** (SVGOverlay는 SVG 좌표계가 이미지와 같으므로 그대로 사용.)
