apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault
  namespace: vault
spec:
  parentRefs:
  - name: public-gateway
    namespace: gateway
  hostnames:
  - vault.yongun.shop
  rules:
    - backendRefs:
      - name: vault-ui
        port: 8200