apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  parentRefs:
  - name: public-gateway
    namespace: gateway
  hostnames:
  - argocd.yongun.shop
  rules:
    - backendRefs:
      - name: argocd-server
        port: 80