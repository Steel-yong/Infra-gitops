# 밤샘 작업 보고서 — 2026-05-26 밤 → 27 새벽

> 어제 취침 전 지시: ① 태이고 재분석 → 에란겔처럼 완성 ② 자율 루프(작업큐) 진행. 그에 대한 보고.
> 브랜치: `feature/overnight-2026-05-27` (PR 리뷰 후 develop 머지). 상세 로그: 같은 폴더 `2026-05-27-overnight-work-handoff.md` §7·§9.

## ① 태이고 → 에란겔처럼 — ✅ 완료·배포

**원인**: 영상 맵 분류기가 색상 휴리스틱(`R>G×1.2`)을 써서 초록 우세인 태이고를 erangel로 오분류 → 태이고 데이터 폐기. AKAZE 특징점 매칭으로 교체.

**98편 전체 재분석 완주**:

| | 옛(버그) | 재분석 |
|---|---|---|
| 태이고 마커 | 434 | 4,658 |
| 에란겔 마커 | 8,567 | 12,957 |
| **태이고 명당(배포)** | **32** | **526** (S6/A26/B83/C411) |
| 에란겔 명당(배포) | 737 | **955** (S6/A46/B117/C786) |

→ 조건부 명당(P방문|자기장 + Wilson) 에란겔과 동일 방식 재계산, 프론트 재빌드로 배포 완료.

### ⚠ 태이고가 에란겔의 약 절반인 이유 (정직)
- 98편 **전부에 태이고·에란겔 둘 다 등장**(옛날처럼 통째로 빠지는 문제는 없음).
- 영상당 프레임: 에란겔 85.8 vs 태이고 44.0 (≈2:1).
- 대회가 맵을 균등 진행한다면 이 2:1은 **태이고 검출률이 낮은 것**으로 보는 게 맞다(태이고의 균일한 숲·평지 지형 → SIFT 호모그래피 매칭 실패가 에란겔보다 잦음. Codex도 지적). 미라마/론도 프레임이 에란겔로 약하게 오분류돼 에란겔이 과대됐을 가능성도 일부.
- **단, 526 명당이면 추천에 충분**(에란겔 955가 동작하면 526도 동작). 검출률 개선은 후속 과제이며 사용 자체를 막지 않음.

## ② 자율 루프(작업큐) — 부분 진행

운영 규칙(Codex 헤드리스 검수 반복 / feature 브랜치 / 빌드·테스트 후 커밋 / 라이브검증 금지)대로.

- **큐 B 회귀 테스트** ✅ — `useLockedCircle` 유닛테스트 6개 작성·통과(컨테이너 vitest). Codex 검수로 streak 경계 보강.
- **PoC C/E** ✅ — AKAZE inlier: C(줌) 4~29 / E(미니맵) 4 → 둘 다 전체매칭 비현실적. 4번=C arc-only 재설계 권장, E는 다른 접근 필요(handoff §8).
- **D(종료감지 OCR) / 4(기능) / 5(어드민)** — 미착수(턴 기반이라 한 턴 분량 한계).

## 커밋(오버나잇)
- `ff3e7bf` 태이고·에란겔 명당 재생성
- `d4234c8` useLockedCircle 회귀 테스트 6개
- `eccf36c` PoC C/E + 막힌점 기록
- (그 전 다른 작업: `b2912ec` SIFT env게이트, `33e3ccb` 락 풀림 수정)

## 아침에 할 것
1. **라이브 확인**(화면공유): 자기장 검출 3-상태 수정 + 태이고 명당 표시.
2. **다음 큐**: C arc-only 재설계 → D 종료감지 OCR → probe-shots vitest → E 접근 결정.
3. (선택) 태이고 검출률 검증·개선 — 에란겔 프레임 미라마 오염 확인 / 태이고 SIFT 실패율 측정.
